.container-fluid
  .row-fluid
    .span1
      %button#refresh-btn.btn.btn-success= "Refresh"
    .span1
      = form_tag(playlist_stop_path, remote:true) do |form|
        = submit_tag 'stop', class:"btn btn-danger"
    .span1
      = form_tag(playlist_skip_path, remote:true) do |form|
        = submit_tag 'skip', class:"btn btn-warning"
    .span4
      = form_tag(change_volume_path, remote:true) do |form|
        = range_field_tag 'volume', @volumes["current"], in: @volumes['min']..@volumes['max']
        = submit_tag 'change volume'
    .span4
      %div
        %span= 'Now playing: '
        %span.label.label-info
          %span#current_song